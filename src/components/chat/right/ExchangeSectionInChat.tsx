import { Exchange } from "../../../common/interfaces/exchange.interface";

export default function ExchangeSectionInChat({
  exchange,
}: {
  exchange: Exchange;
}) {
  return <div>ExchangeSectionInChat</div>;
}
